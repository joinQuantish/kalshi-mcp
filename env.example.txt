# Server Configuration
PORT=3002
NODE_ENV=development

# Database (Railway PostgreSQL - SEPARATE from Polymarket DB)
DATABASE_URL="postgresql://user:password@host:5432/kalshi_sdk?schema=public"

# Encryption (Generate with: openssl rand -hex 32)
ENCRYPTION_KEY="your-64-char-hex-encryption-key-here"

# Solana RPC Configuration
SOLANA_RPC_URL="https://api.mainnet-beta.solana.com"
SOLANA_RPC_FALLBACK="https://solana-mainnet.g.alchemy.com/v2/your-key"

# DFlow API Configuration
# IMPORTANT: DFlow access is NOT permissionless. You must contact the DFlow team
# at https://dflow.net to request API access before self-hosting.
DFLOW_API_URL="https://quote-api.dflow.net"
DFLOW_METADATA_API_URL="https://pond.dflow.net"
DFLOW_API_KEY="your-dflow-api-key-from-dflow-team"

# Admin Configuration
ADMIN_API_KEY="your-secure-admin-key-here"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=60

